"use strict";

const writeThis = (word, number) => {
  for (let i = 0; i < number; i++) {
    console.log(word);
  }
};
writeThis("patata", 10);
writeThis("Aguacate", 7);
writeThis("Pizza", 50);
